# The features:

  * Adding the iconComponent to simplify the marker customization.
  * Adding the showInformationEvent attribute to the marker component.
  * Direct Facelets support.
  * Enable adding events to the HTMLInfoWindow component using the eventListener component.